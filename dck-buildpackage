#!/bin/bash

DCK_BUILDPACKAGE_PREFIX=$(readlink -f $(dirname $(readlink -f $0)))
DCK_BUILDPACKAGE_LIBDIR=$DCK_BUILDPACKAGE_PREFIX/lib
DCK_BUILDPACKAGE_CFDIR=$DCK_BUILDPACKAGE_PREFIX/cf

. $DCK_BUILDPACKAGE_LIBDIR/main.sh
